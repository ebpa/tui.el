(defun tui-use-state (initial-state)
  "Similar to the React useState hook.  Accepts an INITIAL-STATE value and returns a cons cell containing the current state value and setter function value as (STATE-VALUE . STATE-SETTER)."
  
  )

(defun tui-use-ref ()
  ""
  
  )



(provide 'tui-hooks)
